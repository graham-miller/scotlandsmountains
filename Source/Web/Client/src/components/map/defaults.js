import layers from "./layers";

const defaults = {
    Zoom: 7,
    minZoom: 5,
    maxZoom: 18,
    Center: [56.816922, -4.18265],
    maxBounds: [[47.5, -15], [62.5, 5]],
    BaseLayer: layers[0]
};

export default defaults;